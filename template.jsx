import React from 'react';
import { Icon } from 'Library/components';

// SVG Content
const content = '#SVG_CONTENT';

/**
 * The #ICON_HUMAN_READABLE icon component.
 * @param {Object} props The component properties.
 * @returns {JSX}
 */
const #ICON_COMPONENT_NAME = props => <Icon content={content} {...props} />;

export default #ICON_COMPONENT_NAME;
